// This file is generated by @opensumi/actions

// src/helpers.ts
var createVersionText = (type = "Pre-Release", version, context) => {
  if (!context.payload.comment) {
    return "";
  }
  return `\u{1F389} ${type} version ` + version + " publish successful! You can install prerelease version via `npm install package@" + version + " ` [@" + context.payload.comment.user.login + "](https://github.com/" + context.payload.comment.user.login + ")\n\n```\n" + version + "\n```\n";
};

export {
  createVersionText
};
